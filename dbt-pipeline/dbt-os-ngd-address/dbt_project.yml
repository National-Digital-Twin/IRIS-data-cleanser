name: "dbt_os_ngd_address"
version: "1.0.0"
config-version: 2

profile: "dbt_os_ngd_address"

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
analysis-paths: ["analyses"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]
asset-paths: ["assets"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  dbt_os_ngd_address:
    +materialized: table
